name: Github Action

on:
  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Down
        run: docker compose down --rmi all --remove-orphans

      - name: Up
        run: docker compose up